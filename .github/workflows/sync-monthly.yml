name: Sync Forex Calendar Monthly

on:
  schedule:
    # 1x por semana: Domingo às 02:00 UTC (23:00 Sábado BRT)
    # E no dia 1 de cada mês às 02:00 UTC
    - cron: "0 2 * * 0"
    - cron: "0 2 1 * *"

  # Permite trigger manual no GitHub
  workflow_dispatch:

jobs:
  sync:
    name: Monthly Sync from Forex Factory
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Run Monthly Sync
        run: bun run sync-monthly
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

      - name: Report success
        if: success()
        run: |
          echo "✅ Sincronização mensal concluída com sucesso!"
          echo "📅 Meses atualizados: Atual e Próximo"

      - name: Report failure
        if: failure()
        run: |
          echo "❌ Sincronização mensal falhou!"
          echo "Verifique os logs acima para mais detalhes."
